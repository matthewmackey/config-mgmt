# SEE Trello board for info about getting rid of the `cm` user bootstrap idea
- name: Bootstrap machine for other playbooks to run
  hosts: all
  roles:
    # # Setup default Python w/ virtualenvwrapper installed
    # - name: python
    # Setup `cm` user w/ sudoers access
    - name: bootstrap
